package com.codetask.todolist.util;

import java.util.Map;

public class CodeTaskUtilities {

	public static boolean isNullOrEmptyMap(Map <? , ?> map) {
	    return (map == null || map.isEmpty());
	}
}
